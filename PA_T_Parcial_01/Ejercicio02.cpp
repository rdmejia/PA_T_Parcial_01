#include "Ejercicio02.h"

int Ejercicio02::rob(int houses[], int n)
{
    int houses[] = { 2, 7, 9, 3, 1 };
	int maxMoney = Ejercicio02::rob(houses, sizeof(houses) / sizeof(houses[0]));
	int dp[];

for (int i = 2; i < n; i++)
{
	dp[0] = houses[0];
}
return 0;
    return 0;
}
